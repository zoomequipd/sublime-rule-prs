name: "PR# 3700 - Service abuse: Free provider with SendGrid routing"
description: "Message From header includes a free email provider domain but is routed through SendGrid infrastructure, indicating potential service abuse for delivery evasion."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and sender.email.domain.domain in $free_email_providers
  and any(headers.domains, .root_domain == "sendgrid.net")
  and not any(ml.nlu_classifier(body.current_thread.text).intents,
              .name == "benign"
  )
  and not any(ml.nlu_classifier(body.current_thread.text).topics,
              .name == "Bounce Back and Delivery Failure Notifications"
  )
  
tags:
  - "Attack surface reduction"
  - pr_author_zoomequipd
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Free email provider"
  - "Evasion"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
id: "4508b01e-313c-537c-b0ba-375a84cefb11"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3700