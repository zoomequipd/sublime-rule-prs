name: "PR# 3426 - Service Abuse: ExactTarget with suspicious sender indicators"
description: "Message originates from ExactTarget infrastructure but uses a suspicious sender domain, including overly long salesforce.com domains, awsapps.com domains, domains containing UTF-8 encoding characters, or a suspicious sender display name."
type: "rule"
severity: "high"
source: |
  type.inbound
  and any(headers.domains, .root_domain == 'exacttarget.com')
  and (
    (
      length(sender.email.email) >= 50
      and sender.email.domain.root_domain == "salesforce.com"
    )
    or sender.email.domain.root_domain == "awsapps.com"
    or strings.icontains(sender.email.domain.domain, '?utf-8')
    or regex.icontains(sender.display_name,
                       '.*\|.*(Manager|Careers|Recruitment|Specialist|Global)'
    )
  )

attack_types:
  - "Credential Phishing"
  - "BEC/Fraud"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
id: "c74ade56-8c3f-590a-882d-24102a1075b9"
tags:
  - created_from_open_prs
  - rule_status_modified
  - pr_author_peterdj45
references:
  - https://github.com/sublime-security/sublime-rules/pull/3426