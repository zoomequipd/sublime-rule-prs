name: "PR# 3779 - Suspicious sender: Phone reference with Gmail domain"
description: "Detects messages from Gmail senders with display names containing phone number references, with no attachments or links present."
type: "rule"
severity: "low"
source: |
  type.inbound
  and (
    strings.icontains(sender.display_name, 'phone')
    or strings.icontains(sender.display_name, 'number')
  )
  and sender.email.domain.domain == 'gmail.com'
  and length(attachments) == 0
  and length(body.current_thread.links) == 0

attack_types:
  - "BEC/Fraud"
  - "Callback Phishing"
tactics_and_techniques:
  - "Free email provider"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "Sender analysis"
id: "65a651ac-347d-51ab-8f67-6ff4080d573b"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/3779