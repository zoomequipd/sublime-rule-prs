name: "PR# 2882 - Open Redirect: queue.swytchbike.com"
description: "Message contains use of the queue.swytchbike.com open redirect. This has been exploited in the wild."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and any(body.links,
      .href_url.domain.domain == "queue.swytchbike.com"
      and strings.icontains(.href_url.path, '/order')
      and regex.icontains(.href_url.query_params, '(?:^|&)target=')
  )
  and sender.email.domain.root_domain != 'swytchbike.com'
  and headers.return_path.domain.root_domain != "swytchbike.com"
  
  // negate highly trusted sender domains unless they fail DMARC authentication
  and (
    (
      sender.email.domain.root_domain in $high_trust_sender_root_domains
      and not headers.auth_summary.dmarc.pass
    )
    or sender.email.domain.root_domain not in $high_trust_sender_root_domains
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Social engineering"
  - "Open redirect"
detection_methods:
  - "Header analysis"
  - "URL analysis"
  - "Sender analysis"
id: "0fed8952-fb04-5d76-a055-b7adcc7a0b1c"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_aidenmitchell
references:
  - https://github.com/sublime-security/sublime-rules/pull/2882