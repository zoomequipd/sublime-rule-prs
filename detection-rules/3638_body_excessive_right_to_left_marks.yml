name: "PR# 3638 - Body contains excessive right-to-left mark characters"
description: "Detects messages containing right-to-left mark (RLM) characters used to obfuscate non-Arabic/Hebrew text, where RLM characters comprise at least 20% of the extracted content."
type: "rule"
severity: "low"
source: |
  type.inbound
  and length(body.current_thread.text) < 4000
  and not regex.icontains(body.current_thread.text, "[\\p{Arabic}\\p{Hebrew}]")
  and any(regex.iextract(body.current_thread.text, "‏.*‏"),
          not regex.icontains(.full_match, "^[‏ 0-9/]+$")
          and strings.count(.full_match, "\u{200F}") / (length(.full_match) * 1.0) >= 0.20
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
detection_methods:
  - "Content analysis"
id: "60247abc-fa90-5d2f-8300-bfdcb7f7c9e5"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/3638