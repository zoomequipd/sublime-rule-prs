name: "PR# 3544 - Headers: Invalid recipient domain with mismatched reply-to from new sender"
description: "Message sent to an invalid recipient domain with a reply-to address that differs from the sender address, originating from a new sender."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and length(recipients.to) <= 1
  and all(recipients.to, .email.domain.valid == false)
  and length(headers.reply_to) == 1
  and headers.reply_to[0].email.email != sender.email.email
  and profile.by_sender_email().prevalence == "new"
tags:
  - "Attack surface reduction"
  - pr_author_zoomequipd
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "BEC/Fraud"
  - "Credential Phishing"
  - "Spam"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Header analysis"
  - "Sender analysis"
id: "6184961f-5afa-5a2b-8229-d27e03770ec2"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3544