name: "PR# 3443 - Service abuse: Facebook business with action required subject"
description: "Detects messages from the Facebook business domain containing 'action required' in the subject line, commonly used to create urgency in impersonation attacks."
type: "rule"
severity: "medium"
source: |
  type.inbound
  and sender.email.email == "noreply@business.facebook.com"
  and (
      // and the link is recently registered
    any(body.links, network.whois(.href_url.domain).days_old <= 30)
    or any(body.links,
           // if the link is still active, check if it's cred theft
           any(ml.nlu_classifier(beta.ocr(ml.link_analysis(.).screenshot).text).intents,
               .name == "cred_theft" and .confidence != "low"
           )
    )
    // or look for the legit Meta footer address
    or strings.icontains(body.plain.raw, '1 Meta Way, Menlo Park, CA 94025')
  )
tags:
  - "Attack surface reduction"
  - pr_author_IndiaAce
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Impersonation: Brand"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "Sender analysis"
id: "eda7e455-3904-5c92-b7ee-907783230093"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3443