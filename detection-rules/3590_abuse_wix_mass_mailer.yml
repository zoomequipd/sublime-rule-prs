name: "PR# 3590 - Service abuse: Wix redirect through bulk mailer domains"
description: "Detects messages containing Wix-encoded links that redirect through bulk mailing service domains, potentially bypassing security controls through legitimate redirect services."
type: "rule"
severity: "low"
source: |
  type.inbound
  and any(filter(body.links, 'wix' in .href_url.rewrite.encoders),
          .href_url.domain.root_domain in $bulk_mailer_url_root_domains
  )
tags:
  - "Attack surface reduction"
  - pr_author_D-Bolton
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Credential Phishing"
  - "Malware/Ransomware"
tactics_and_techniques:
  - "Evasion"
  - "Open redirect"
detection_methods:
  - "URL analysis"
  - "Sender analysis"
id: "075168c8-21a1-55ff-ae44-b25e0485f15e"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3590