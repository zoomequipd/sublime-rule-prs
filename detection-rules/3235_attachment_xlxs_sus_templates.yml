name: "PR# 3235 - Attachment: Excel File With Suspicious Template Identifier"
description: "Detects Excel attachments containing a specific template identifier (TM16390866) in the EXIF metadata, which may indicate malicious or suspicious document templates being used to distribute harmful content."
type: "rule"
severity: "high"
source: |
  type.inbound
  and any(attachments,
      .file_type == "xlsx"
      and any(beta.parse_exif(.).fields,
              .key == "Template" and .value == "TM16390866"
      )
  )

attack_types:
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Macros"
detection_methods:
  - "Exif analysis"
  - "File analysis"
id: "a3a60718-1952-577e-8e22-28b950dbe636"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_zoomequipd
references:
  - https://github.com/sublime-security/sublime-rules/pull/3235