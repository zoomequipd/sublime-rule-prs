name: "PR# 3766 - Link: Breely link masquerading as PDF"
description: "Detects messages containing a single Breely link that displays as a PDF file. Typically, redirects to a different destination for malicious purposes."
type: "rule"
severity: "high"
source: |
  type.inbound
  and length(filter(body.links, .href_url.domain.root_domain == "breely.com")) == 1
  and any(body.links,
          .href_url.domain.root_domain == "breely.com"
          and strings.icontains(.display_text, ".pdf")
  )

attack_types:
  - "BEC/Fraud"
  - "Credential Phishing"
tactics_and_techniques:
  - "Free subdomain host"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "URL analysis"
id: "a68667c8-2ca4-50dc-886c-2d0f0ff7cc29"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_MSAdministrator
references:
  - https://github.com/sublime-security/sublime-rules/pull/3766