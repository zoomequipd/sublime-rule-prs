name: "PR# 3616 - Subject and sender display name contains matching long alphanumeric string"
description: "Detects messages where both the subject line and sender display name contain identical alphanumeric strings that are between 32 and 64 characters, which may indicate automated generation or coordination between these fields for malicious purposes."
type: "rule"
severity: "low"
source: |
  type.inbound
  and any(regex.iextract(subject.subject, '[^a-z0-9](?P<string>[a-z0-9]{32,64})(?:$|[^a-z0-9])'),
         strings.contains(sender.display_name, .named_groups["string"])
  )

tags:
  - "Attack surface reduction"
  - pr_author_D-Bolton
  - created_from_open_prs
  - rule_status_added
attack_types:
  - "Malware/Ransomware"
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "Header analysis"
  - "Sender analysis"
id: "da5cb864-7ca5-5d52-84af-9f3b4e841197"
references:
  - https://github.com/sublime-security/sublime-rules/pull/3616