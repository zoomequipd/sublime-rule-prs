name: "PR# 3616 - Subject and sender display name contain matching 32-character alphanumeric string"
description: "Detects messages where both the subject line and sender display name contain identical 32-character alphanumeric strings, which may indicate automated generation or coordination between these fields for malicious purposes."
type: "rule"
severity: "low"
source: |
  type.inbound
  and any(regex.extract(subject.subject, '[A-Za-z0-9]{32}'),
          any(regex.extract(sender.display_name, '[A-Za-z0-9]{32}'),
              ..full_match == .full_match
          )
  )
  and (
    any(ml.nlu_classifier(body.current_thread.text).intents,
        .name == 'cred_theft' and .confidence == 'high'
    )
    or strings.contains(body.current_thread.text, "\u{200F}")
  )

attack_types:
  - "Malware/Ransomware"
  - "Credential Phishing"
tactics_and_techniques:
  - "Evasion"
  - "Social engineering"
detection_methods:
  - "Content analysis"
  - "Header analysis"
  - "Sender analysis"
id: "da5cb864-7ca5-5d52-84af-9f3b4e841197"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_D-Bolton
references:
  - https://github.com/sublime-security/sublime-rules/pull/3616