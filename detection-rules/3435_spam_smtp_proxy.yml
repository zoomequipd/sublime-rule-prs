name: "PR# 3435 - Spam: SMTP & Proxy Communications in Email Body"
description: |
  An email containing SMTP and Proxy (socks5) command and control information within the body of the message.
type: "rule"
severity: "medium"
source: |
  type.inbound
  and length(attachments) == 0
  and 1 < length(body.links) < 5
  and regex.icontains(body.current_thread.text,
                      ":socks5:|::127.0.0.1:1080|:0:127.0.0.1:1080"
  )

attack_types:
  - "Spam"
tactics_and_techniques:
  - "Free email provider"
detection_methods:
  - "Content analysis"
id: "1f5fb649-ecb7-57a4-ac2a-a1071acd7b30"
tags:
  - created_from_open_prs
  - rule_status_added
  - pr_author_MSAdministrator
references:
  - https://github.com/sublime-security/sublime-rules/pull/3435